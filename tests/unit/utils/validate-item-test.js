import validateItem from 'dummy/utils/validate-item';
import { module, test } from 'qunit';

module('Unit | Utility | validate item');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = validateItem();
  assert.ok(result);
});
