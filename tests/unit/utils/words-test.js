import words from 'dummy/utils/words';
import { module, test } from 'qunit';

module('Unit | Utility | words');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = words();
  assert.ok(result);
});
