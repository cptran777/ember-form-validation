import numbers from 'dummy/utils/numbers';
import { module, test } from 'qunit';

module('Unit | Utility | numbers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numbers();
  assert.ok(result);
});
