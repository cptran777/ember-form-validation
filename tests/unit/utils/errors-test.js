import errors from 'dummy/utils/errors';
import { module, test } from 'qunit';

module('Unit | Utility | errors');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = errors();
  assert.ok(result);
});
