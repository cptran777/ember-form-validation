import dates from 'dummy/utils/dates';
import { module, test } from 'qunit';

module('Unit | Utility | dates');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dates();
  assert.ok(result);
});
